#include <iostream>

using namespace std;

int main()
{
    int n;
    
    cin >> n;
    
    if(n % 2 == 1 or (n % 2 == 0 and n <= 20 and n >= 6))
    {
        cout << "Weird";
    }
    else
    {
        cout << "Not Weird";
    }

    return 0;
}
